---
description: "Crawl university admission/program data from a URL. Supports single detail page, single index page, or paginated multi-page crawl."
disable-model-invocation: true
---

Invoke the uni-admission-agent:using-uni-admission-agent skill, run its preflight, and route to uni-admission-crawl. Use the user's message that triggered this command as the crawl request.
